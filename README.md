# hackharvard2024

# Installation

* `brew install portaudio`
* `brew install ffmpeg`
* `pip install -r requirements.txt`
* .env variables needed
    * `OPENAI_API_KEY`
    * `RECORDING_FOLDER`
    * `WHISPER_MODEL`